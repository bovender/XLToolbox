<Window x:Class="Bovender.Mvvm.Views.ProcessView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        Title="{Binding Caption}" ResizeMode="NoResize"
    >
    <StackPanel Margin="10">
        <ProgressBar Value="{Binding Content.PercentCompleted}" IsIndeterminate="{Binding Content.IsIndeterminate}"
                     Width="300" Height="20" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button IsCancel="True" Content="{Binding CancelText}" Padding="10,5,10,5" />
        </StackPanel>
    </StackPanel>
</Window>
