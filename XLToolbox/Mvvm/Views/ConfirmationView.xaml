<Window x:Class="XLToolbox.Mvvm.Views.ConfirmationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:settings="clr-namespace:Bovender.Mvvm.Views.Settings;assembly=Bovender"
        xmlns:l="clr-namespace:XLToolbox"
        SizeToContent="WidthAndHeight"
        settings:WindowState.CenterScreen="True"
        Title="{Binding Path=Caption}">
    <Window.Resources>
        <ResourceDictionary Source="/XLToolbox;component/Style.xaml" />
    </Window.Resources>
    <StackPanel Margin="10">
        <TextBlock Text="{Binding Message}" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Command="{Binding Path=Content.ConfirmCommand}" Content="{x:Static l:Strings.OK}" Margin="0 0 5 0" />
            <Button Command="{Binding Path=Content.CloseViewCommand}" Content="{x:Static l:Strings.Cancel}" />
        </StackPanel>
    </StackPanel>
</Window>
